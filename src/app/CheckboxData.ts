export interface CheckboxData {
  id: number;
  label: string;
  checked: boolean;
  value: boolean;
  title: string;
  body: string;
  userId: number

  // id: number;
  // label: string;
  // checked?: boolean;  
  // value: boolean;
  // title: string;
}
